   // Navigating to the screen with parameters
   navigation.navigate('ScreenName', { param1: 'value1', param2: 'value2' });

   // Accessing the parameters within the screen component
   const { param1, param2 } = route.params;
   